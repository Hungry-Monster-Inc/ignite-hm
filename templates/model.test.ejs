import { <%= props.pascalName %>Model, <%= props.pascalName %> } from "app/models/<%= props.name %>/<%= props.name %>"

test("can be created", () => {
  const instance: <%= props.pascalName %> = <%= props.pascalName %>Model.create({})

  expect(instance).toBeTruthy()
})
