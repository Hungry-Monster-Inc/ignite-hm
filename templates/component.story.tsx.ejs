import * as React from "react"
import { storiesOf } from "@storybook/react-native"
import { StoryScreen, Story, UseCase } from "app/../storybook/views"
import { color } from "app/theme"
import { <%= props.pascalName %> } from "app/components/<%= props.kebabCaseName %>/<%= props.kebabCaseName %>"

storiesOf("<%= props.pascalName %>", module)
  .addDecorator(fn => <StoryScreen>{fn()}</StoryScreen>)
  .add("Style Presets", () => (
    <Story>
      <UseCase text="Primary" usage="The primary.">
        <<%= props.pascalName %> style={{ backgroundColor: color.error }} />
      </UseCase>
    </Story>
  ))
